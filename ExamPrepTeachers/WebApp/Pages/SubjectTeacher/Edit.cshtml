@page
@model ExamPrepTeachers.Pages_SubjectTeacher.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>SubjectTeacher</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="SubjectTeacher.Id" />
            <div class="form-group">
                
                <label asp-for="SubjectTeacher.TeacherId" class="control-label"></label>
                <select asp-for="SubjectTeacher.TeacherId" class="form-control" asp-items="ViewBag.TeacherId"></select>
                <span asp-validation-for="SubjectTeacher.TeacherId" class="text-danger"></span>
            </div>
            <div class="form-group">
                
                <label asp-for="SubjectTeacher.SubjectId" class="control-label"></label>
                <select asp-for="SubjectTeacher.SubjectId" class="form-control" asp-items="ViewBag.SubjectId"></select>
                <span asp-validation-for="SubjectTeacher.SubjectId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
